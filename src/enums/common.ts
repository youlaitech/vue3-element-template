export enum DialogMode {
  CREATE = "create",
  EDIT = "edit",
  VIEW = "view",
}

export enum CommonStatus {
  DISABLED = 0,
  ENABLED = 1,
}

export enum AuditStatus {
  PENDING = 0,
  APPROVED = 1,
  REJECTED = 2,
}
